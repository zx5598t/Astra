# START HERE — ASTRA 0.2.0

## 1. 실행

**가장 쉬운 방법**: Releases에서 `ASTRA-0.2.0-windows.zip`을 받아 `ASTRA.exe` 실행.

**소스로 실행**:
1. Godot 4.7.x Windows(Standard)를 받아 압축을 풉니다. (다운로드 폴더에 두면 자동으로 찾습니다)
2. 이 폴더의 `run_astra.bat` 더블클릭.
3. Godot을 못 찾는다는 안내가 나오면, 이 폴더에 `godot_path.txt`를 만들고 아래처럼 한 줄 적은 뒤 다시 실행합니다.
   ```
   C:\Users\내이름\Downloads\Godot_v4.7.2-stable_win64.exe
   ```

창이 화면보다 크면 자동으로 줄어듭니다. `F11`은 전체 화면입니다.

## 2. 첫 판 가이드 (DEAD AIR, 약 15분)

1. **타이틀**: 조사 방식은 처음엔 **분석관**을 추천합니다. `조사 시작`.
2. **브리핑**: 두 조작의 시각과 장소를 봅니다. 사건 시간대(예: 07:36~07:39)는 화면 위에 계속 표시됩니다.
3. **현장 조사**: 조작 현장(빨간 표시) 두 곳과 나머지 구역을 골고루 조사합니다.
   - `흔적` 단서는 후보 명단을 줍니다. 예) 내열 섬유: Rho, Dax / 기술 권한: Rho, Vale, Eli → 둘 다에 있는 **Rho**.
   - 오른쪽 **추리 노트** 탭에서 조작별 교차표가 자동으로 그려집니다. 모든 흔적에 들어간 이름이 금색으로 표시됩니다.
4. **개인 심문**: 누군가 면담을 요청하면 먼저 답합니다. 그다음 알리바이를 묻습니다.
   - `출입 기록`과 진술이 어긋나면 노트에 **모순**이 생기고, `모순을 추궁한다`가 열립니다.
   - 무고한 사람도 사정이 있어 거짓말을 할 수 있습니다. 신뢰를 쌓고 추궁하면 털어놓습니다.
5. **공개 회의**: 모두의 알리바이가 공개되고, 같은 장소에 있던 사람들이 서로 반박합니다.
   - 명단에서 사람을 고른 뒤 `지목` / `변호`. 공개된 근거가 있어야 설득됩니다. `단서 공개`로 근거를 먼저 깔아 두세요.
   - 명단 카드에 각자의 **투표 의향**이 보입니다.
6. **격리 투표**: 명단에서 대상을 고르고 투표합니다. 두 명을 `N`으로 표시해 두면 추리 보고서가 함께 제출됩니다.
7. **밤**: 한 명을 보호하거나 구역을 감시합니다. 습격을 막으면 범인의 흔적이 새 단서로 남습니다.

막히면 `Esc` → **플레이 방법**.

## 3. 캠페인

- DEAD AIR를 한 번 끝까지 조사하면 GLASS GARDEN, 그다음 ECHO WARD가 열립니다.
- 뒤 사건일수록 **사건 시간대 밖의 무관한 흔적**이 섞이고, Null끼리 알리바이를 맞추고, 흔적을 더 적극적으로 지웁니다.
- 기록은 `%APPDATA%\Godot\app_userdata\ASTRA\astra_meta.cfg`에 저장됩니다. (설정 → 조사 기록 초기화로 지울 수 있음)

## 4. 개발자라면

- 규칙과 구조: `AGENTS.md`, `docs/GAME_DESIGN.md`
- 테스트: `godot --headless --path . --script res://tests/run_tests.gd`
